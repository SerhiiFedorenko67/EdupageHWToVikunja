# Project docs

- `edupage-api.md` — reverse-engineered EduPage endpoints (login, eqap encoding, RPC pattern, homework timeline). Inspiration only; the third-party library is **not** a dependency.
- `vikunja-api.md` — the live Vikunja v2 API (`/api/v2`), auth, pagination, rich-text/Markdown conventions, endpoint map.
- `application.md` — original design plus current implementation coverage and remaining live verification: EduPage homework → tasks in one Vikunja project.
